# RocketMQ Docker 快速启动指南

## 🎯 一键启动（推荐）

### Windows 用户

```powershell
# 1. 进入目录
cd docs\dev-ops\rocketmq

# 2. 启动 RocketMQ
.\start-rocketmq.bat start

# 3. 查看状态（等待约 30 秒）
.\start-rocketmq.bat status
```

### Linux/Mac 用户

```bash
# 1. 进入目录
cd docs/dev-ops/rocketmq

# 2. 添加执行权限
chmod +x start-rocketmq.sh

# 3. 启动 RocketMQ
./start-rocketmq.sh start

# 4. 查看状态（等待约 30 秒）
./start-rocketmq.sh status
```

## ✅ 验证安装

### 1. 检查容器状态

```bash
docker ps
```

应该看到 3 个容器正在运行：
- `rocketmq-namesrv`
- `rocketmq-broker`
- `rocketmq-dashboard`

### 2. 访问管理控制台

打开浏览器访问：http://127.0.0.1:18080

如果能看到 RocketMQ Dashboard 界面，说明部署成功！

### 3. 查看集群状态

在 Dashboard 中点击 "集群" 菜单，应该能看到：
- 1 个 NameServer
- 1 个 Broker

## 🔧 基本操作

### 查看日志

```bash
# Windows
.\start-rocketmq.bat logs

# Linux/Mac
./start-rocketmq.sh logs
```

### 停止服务

```bash
# Windows
.\start-rocketmq.bat stop

# Linux/Mac
./start-rocketmq.sh stop
```

### 重启服务

```bash
# Windows
.\start-rocketmq.bat restart

# Linux/Mac
./start-rocketmq.sh restart
```

## 📝 测试发送消息

### 方式一：使用 Dashboard

1. 访问 http://127.0.0.1:18080
2. 点击 "消息" → "发送消息"
3. 输入 Topic 名称（如：`test-topic`）
4. 输入消息内容
5. 点击 "发送"

### 方式二：使用命令行

```bash
# 进入 Broker 容器
docker exec -it rocketmq-broker bash

# 发送测试消息
sh bin/tools.sh org.apache.rocketmq.example.quickstart.Producer

# 接收测试消息
sh bin/tools.sh org.apache.rocketmq.example.quickstart.Consumer
```

## 🎓 下一步

1. 查看完整文档：`README.md`
2. 集成到你的 Java 项目
3. 学习 RocketMQ 高级特性

## ❓ 遇到问题？

### 容器无法启动

```bash
# 查看容器日志
docker logs rocketmq-namesrv
docker logs rocketmq-broker
docker logs rocketmq-dashboard
```

### 端口被占用

检查以下端口是否被占用：
- 9876 (NameServer)
- 10909, 10911, 10912 (Broker)
- 18080 (Dashboard)

```bash
# Windows
netstat -ano | findstr "9876"

# Linux/Mac
lsof -i :9876
```

### 重新部署

```bash
# Windows
.\start-rocketmq.bat clean
.\start-rocketmq.bat start

# Linux/Mac
./start-rocketmq.sh clean
./start-rocketmq.sh start
```

## 📞 获取帮助

```bash
# Windows
.\start-rocketmq.bat

# Linux/Mac
./start-rocketmq.sh
```

---

**祝你使用愉快！** 🚀

